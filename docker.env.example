# Docker Environment Configuration for LGU-Chat

# JWT Secret - REQUIRED: Generate a secure random string for production!
# Generate with: openssl rand -base64 32
JWT_SECRET=

# Server Configuration
NODE_ENV=production
PORT=3000
SERVER_HOST=0.0.0.0

# Domain configuration - Production domain for LGU Quezon
DOMAIN_NAME=chat.lguquezon.local

# Custom allowed IPs for network access (optional)
# Comma-separated list of IPs that can access the chat
# Example: CUSTOM_ALLOWED_IPS=192.168.1.100,10.0.0.50
CUSTOM_ALLOWED_IPS=

# Socket.IO URL - Production URL
NEXT_PUBLIC_SOCKET_URL=http://chat.lguquezon.local

# CORS allowed origins - Add additional domains if needed
ALLOWED_ORIGINS=http://chat.lguquezon.local

# JWT Token Expiration (optional, defaults to 24h)
JWT_EXPIRES_IN=24h

# Production Security Settings
# Uncomment and configure for HTTPS in production:
# DOMAIN_NAME=chat.lguquezon.local
# NEXT_PUBLIC_SOCKET_URL=https://chat.lguquezon.local
# ALLOWED_ORIGINS=https://chat.lguquezon.local 